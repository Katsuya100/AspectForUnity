namespace Katuusagi.AspectForUnity
{
    // This interface is consumed by generated IL; it is not exposed by advice signatures.
    public interface IProceedingState
    {
        void Proceed(int slot, int generation);
    }
}
